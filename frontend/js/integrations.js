// Job Radar - integrations workstream front-end module (placeholder).
//
// Loaded as an ordered classic <script> immediately after app.js and before the
// other feature modules, so window.JobRadar is already defined when this runs.
// Intentionally empty (IIFE shell + comments only) until the integrations
// workstream lands. Do not add interface logic here in the prelude.
(function () {
  "use strict";
})();
