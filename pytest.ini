[pytest]
# repo root on sys.path so `import backend` works from tests/
pythonpath = .
testpaths = tests
addopts = -q -ra
# No filterwarnings: DeprecationWarning and friends stay visible in the summary.
