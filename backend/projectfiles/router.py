"""Project-file HTTP routes (skeleton).

Exposes an empty ``APIRouter`` so ``backend.main`` can mount it unconditionally.
No routes are registered yet.
"""

from fastapi import APIRouter

router = APIRouter()
