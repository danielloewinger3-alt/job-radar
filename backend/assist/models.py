"""Assist / Alfred SQLModel tables.

Skeleton for the prelude: no tables are declared yet. Startup imports this module
(via ``backend.features.import_feature_models``) so that any tables added here
later are registered on ``SQLModel.metadata`` before ``create_all()``.
"""
